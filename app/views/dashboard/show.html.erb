<div class="container-fluid">
  <h1>Welcome, <%= current_user.name %>.</h1>
  <%= link_to "Logout", logout_path, method: :delete %>
  <%= image_tag current_user.image_url, width: 230, height: 230, class: "img-rounded" %>
  <%= "Login Name:  #{@github.login_name}" %>
</div>
